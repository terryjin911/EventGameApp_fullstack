자바스크립트 자료형
https://developer.mozilla.org/ko/docs/Web/JavaScript/Data_structures


일단 기초데이터로 1트 ㄱㄱ
state.product.products   products가 initialState 안에 들어있는 배열
state.score.score

import { insertProduct } from "../../modules/product";
insertProduct

import { setEntry, setUserInfo} from "어쩌구 ";
setEntry
setUserInfo


아 다 아니었고
const globalScore = useSelector((state) => state.entry.users);
이거였음ㅋㅋㅋ                                                           ㄴ얘가 product 그새끼였음 아 진짜 너무개운하다 아!!!!!!!!!!!!!!
  const globalScore = useSelector((state) => state.entry.score);


  //전역데이터 공간에서 product
  //기초 데이터를 불러오는 기능
  //const globalProducts = useSelector((state) => state.product.products); 


 //리덕스 전역데이터가 변경되는 시점에서 무언가 추가작업을 하고 싶을떄
  const globalProducts = useSelector(
    (state) => state.product.products,
    (newitem, previousItem) => {
      console.log(
        "신규 상품이 추가 또는 변경되었습니다.",
        newitem,
        previousItem
      );
      //추가작업 진행
      //setUseState('샘플');
    }
  );
